import { Cloud, HomeIcon, LucideIcon, NotepadText, Waypoints } from "lucide-react";

export type Icon = LucideIcon

export const Icons = {
    dashboard: HomeIcon,
    Cloud: Cloud,
    Notepad: NotepadText,
    Waypoint: Waypoints
}