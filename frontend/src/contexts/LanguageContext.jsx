import { createContext } from "react";

/**
 * Global context for language management.
 * Exporting from a separate file to ensure ESLint "Fast Refresh" compatibility.
 */
export const LanguageContext = createContext();
